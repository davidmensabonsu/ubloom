Add Ubi Routine Integration to Premium Features List

Update the `features` array in `src/pages/Upgrade.tsx` to include a new bullet highlighting Ubi's ability to directly add items to the user's daily routine plan.

```
Before:
  'Priority access to new features',

After:
  'Priority access to new features',
  'Unlimited Ubi routine integration — Ubi adds items directly to your daily plan',
```

No other changes needed.